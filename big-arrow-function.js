// onek gula kaj eksathe ekti function er maddhome korar jonno arrow function diye kora hoy

// eki parameter diye ekbar + korte hobe erpor ekbar - korte hobe ekbar * korte hobe ekbar / korte hobe erpor output dekhate hobe 5 gun kore-------

// const doMath = (x,y)=>{

//     const sum = x+y;

//     const diff = x-y;

//    const multiple = sum * diff;

//    const division = multiple / 2;

//     const output = division * 5;

//     return output;
// }

// const finalResult = doMath(30,20);

// console.log(finalResult)



const first = (x,y) =>{

    const sum = (x+y);

    const minus = (x-y);

    const multiply = (x * y);

    const number3Multiply = (sum * minus * multiply);

    const divided = number3Multiply / 2;

    const multiply2Divided = divided * 10;

    return multiply2Divided;
}

const result = first(20, 10);

console.log(result)

